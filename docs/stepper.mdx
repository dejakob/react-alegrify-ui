# Footer

import Preview from '../build/docs-components/preview';
import PropsTable from '../build/docs-components/props-table';
import Stepper from '../build/stepper';

## Variants

### Default

<Preview
    code={`
        <Stepper
            progress={50}
            steps={['Step 1', 'Step 2', 'Step 3', 'Step 4']}
        />
    `}
>
    <Stepper
        progress={50}
        steps={['Step 1', 'Step 2', 'Step 3', 'Step 4']}
    />
</Preview>
