# Main

import Preview from '../build/docs-components/preview';
import PropsTable from '../build/docs-components/props-table';
import Main from '../build/main';

The Main component can be used as a page container.

It fills on smaller viewports and centers on larger viewports.